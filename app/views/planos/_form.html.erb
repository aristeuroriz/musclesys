<%= form_for(@plano) do |f| %>
<% if @plano.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@plano.errors.count, "error") %> prohibited this plano from being saved:</h2>

  <ul>
    <% @plano.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :desc_plano %><br>
  <%= f.text_field :desc_plano %>
</div>
<div class="field">
  <%= f.label :valor_plano %><br>
  <%= f.text_field :valor_plano %>
</div>
<div class="field">
  <%= f.label :promo_id %><br>
  <%= f.text_field :promo_id %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
